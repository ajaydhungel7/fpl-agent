# AWS Bedrock Configuration Template for FPL Assistant
# Copy this to .env and fill in your values

# =============================================================================
# FPL Configuration
# =============================================================================
FPL_TEAM_ID=your_fpl_team_id_here

# =============================================================================
# AWS Bedrock Configuration
# =============================================================================

# AWS Region (choose one with Bedrock access)
# Recommended: us-east-1 (has all models)
# Other options: us-west-2, eu-west-1, ap-southeast-1
AWS_REGION=us-east-1

# AWS Access Credentials
# Get these from IAM Console after creating a user with Bedrock access
AWS_ACCESS_KEY_ID=AKIA...your_access_key_id_here...
AWS_SECRET_ACCESS_KEY=your_secret_access_key_here

# =============================================================================
# Model Selection (Optional)
# =============================================================================

# Uncomment to specify which Claude model to use on Bedrock
# If not specified, Strands will use the default (usually Claude 3 Sonnet)

# Claude 3.5 Sonnet (Recommended - Best performance)
MODEL=anthropic.claude-3-5-sonnet-20241022-v2:0

# Claude 3 Sonnet (Good balance)
# MODEL=anthropic.claude-3-sonnet-20240229-v1:0

# Claude 3 Haiku (Fastest, cheapest)
# MODEL=anthropic.claude-3-haiku-20240307-v1:0

# =============================================================================
# Alternative: AWS Session Token (for temporary credentials)
# =============================================================================
# If using AWS SSO or temporary credentials:
# AWS_SESSION_TOKEN=your_session_token_here

# =============================================================================
# Notes
# =============================================================================
#
# 1. Never commit this file to git (it's in .gitignore)
# 2. Ensure your IAM user has "bedrock:InvokeModel" permission
# 3. Request model access in Bedrock console before use
# 4. Monitor costs in AWS Cost Explorer
#
# See AWS_BEDROCK_SETUP.md for detailed setup instructions
